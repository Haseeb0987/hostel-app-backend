services:
  - type: web
    name: hostel-app-backend
    runtime: node
    runtimeVersion: 18
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
    autoDeploy: false
